[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    integracao_real: chama uma API de IA de verdade (custo/latência real) — não roda no pytest padrão. Rode manualmente com: pytest -m integracao_real -o addopts=""
addopts = -m "not integracao_real"
